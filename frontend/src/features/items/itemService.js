// import axios from 'axios';

// const API_URL = 'http://localhost:5000/api/items/';

// // Create an item
// // const createItem = async (itemData, token, currentCampaign) => {
// //     const tokenHeader = {
// //         headers: {
// //             Authorization: `Bearer ${token}`,
// //         },
// //     };

// //     const response = await axios.post(API_URL, { shopData, currentCampaign }, tokenHeader);

// //     return response.data;
// // };

// // Get an item
// const getItem = async (itemList, token) => {
//     const tokenHeader = {
//         headers: {
//             Authorization: `Bearer ${token}`,
//         },
//     };

//     const response = await axios.get(`${API_URL}?itemList=${itemList}`, tokenHeader);
//     console.log(response.data);
//     return response.data;
// };

// const itemService = {
//     getItem,
// };

// export default itemService;
// // , { params: { currentCampaign } }
